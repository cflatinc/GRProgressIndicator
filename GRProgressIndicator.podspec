Pod::Spec.new do |s|
  s.name = "GRProgressIndicator"
  s.version = "1.0"
  s.source = {git: "https://github.com/cflatinc/GRProgressIndicator", tag: "#{s.version}"}
  s.source_files = "GRProgressIndicator/*"
end
